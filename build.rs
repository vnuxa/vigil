// use std::ops::Bound;

use std::env::{self, var};

fn main() {
    // println!("cargo:rustc-link-lib=ghostty");
    //
    // let bindings = bindgen::Builder::default()
    //     .header(var("GHOSTTY_HEADER").unwrap()) // might be a stupid workaround, but it works
    //     .parse_callbacks(Box::new(bindgen::CargoCallbacks::new()))
    //     .generate()
    //     .expect("unable to generate ghostty c bindings to rust");
    //
    // bindings
    //     .write_to_file("src/runtimes/libghostty/auto_gen/bindings.rs")
    //     .expect("unable to generaste bindings");
}
